name: firstpipeline

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout
    - uses: actions/setup-java
      with:
        distribution: 'zulu'
        java-version: '8'
